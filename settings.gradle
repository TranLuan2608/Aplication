dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
        maven { url "https://maven.xyz.com" }
    }
}
rootProject.name = "Application"
include ':app'
include ':application_library'
